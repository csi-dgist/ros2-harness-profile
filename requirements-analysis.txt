# Step 1 of the README (verify the published numbers) needs only these.
# The analyser and the replay payload builder use the standard library plus numpy.
# The figure script additionally needs matplotlib.
# The Profile compiler and the scan-goal test read YAML.
numpy>=1.24
matplotlib>=3.7
pyyaml>=6.0
